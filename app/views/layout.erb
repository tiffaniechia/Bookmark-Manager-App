<!DOCTYPE html> 

<html>
<body>
  <% if flash[:errors] && !flash[:errors].empty? %> #checking if the array exist and that it is not empty
    Sorry, there were the following problems with the form
    <ul id="errors">
      <% flash[:errors].each do |error| %>
       <li><%= error %></li>
      <%end%>
    </ul>
  <%end%>  

  <% if current_user %>
    Welcome, <%=current_user.email%>
  <%end%>

  <%=yield%> 
</body>

</html>